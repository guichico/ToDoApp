package com.apphico.core_model.fakeData

import com.apphico.core_model.FocusMode

val mockedFocus = FocusMode(
    name = "Trabalho",
    timer = 3000000,
    interval = 300000,
    group = mockedGroups[2]
)